name: Headwear publication V5 verification
on:
  push:
    branches: [visioncore-headwear-publication-v5]
permissions:
  contents: write
jobs:
  verify:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: visioncore-headwear-publication-v5
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
      - name: Apply V5 patch
        run: python scripts/headwear-publication-v5-patch.py
      - name: Syntax check
        run: node --check src/intelligence/perceptionV6/index.js
      - name: Targeted headwear gate
        run: NODE_ENV=test node --test test/headwearPublicationV5.test.js test/objectPresenceV1.test.js test/objectPresenceV2Publication.test.js test/objectPresenceV3.test.js test/perceptionConfidenceNormalization.test.js test/perceptionV6Pixel.test.js
      - name: Full suite
        run: npm test
      - name: Persist validated change
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/intelligence/perceptionV6/index.js test/headwearPublicationV5.test.js
          git commit -m 'Require high-confidence headwear publication' || exit 0
          git push
